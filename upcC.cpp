//#include <bits/stdc++.h>
#include <iostream>
#include <cstdio>
#include <cstring>
#include <algorithm>
#include <cmath>
#include <vector>
#define fast_io()                \
    ios::sync_with_stdio(false); \
    std::cin.tie(0);
typedef long long ll;
typedef long double ld;
using namespace std;
ll ans = 0;
ll dfs(ll num,int mul,ll sum)
{
    if(sum>s)
    {

    }


}

int main()
{
    fast_io();
    ll s;
    cin >> s;
    dfs(s/2, 9, 0);
    return 0;
}